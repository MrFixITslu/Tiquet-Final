diff --git a/nginx/nginx.conf b/nginx/nginx.conf
index 8d11257a..5ea3bf03 100644
--- a/nginx/nginx.conf
+++ b/nginx/nginx.conf
@@ -90,6 +90,21 @@ http {
         add_header Referrer-Policy           "strict-origin-when-cross-origin" always;
         add_header Permissions-Policy        "geolocation=(), microphone=(), camera=()" always;
 
+        # ── WebSocket — real-time chat ─────────────────────────────────────────
+        location /ws {
+            proxy_pass         http://app_backend;
+            proxy_http_version 1.1;
+            proxy_set_header   Upgrade          $http_upgrade;
+            proxy_set_header   Connection       "upgrade";
+            proxy_set_header   Host             $host;
+            proxy_set_header   X-Real-IP        $remote_addr;
+            proxy_set_header   X-Forwarded-For  $proxy_add_x_forwarded_for;
+            proxy_set_header   X-Forwarded-Proto https;
+            # WebSockets are long-lived — don't let nginx time them out
+            proxy_read_timeout 3600s;
+            proxy_send_timeout 3600s;
+        }
+
         # ── API routes → Node backend ─────────────────────────────────────────
         location /api/auth/ {
             limit_req zone=auth burst=5 nodelay;
diff --git a/package.json b/package.json
index c36606e8..7673633e 100644
--- a/package.json
+++ b/package.json
@@ -45,9 +45,9 @@
     "recharts": "^3.7.0",
     "speakeasy": "^2.0.0",
     "uuid": "^13.0.0",
-    "vite": "^6.2.0"
+    "vite": "^6.2.0",
+    "ws": "^8.19.0"
   },
-  "devDependencies": {
     "@types/express": "^4.17.21",
     "@types/node": "^22.14.0",
     "@types/qrcode": "^1.5.6",
diff --git a/server/index.js b/server/index.js
index 3360baeb..5bd2ff44 100644
--- a/server/index.js
+++ b/server/index.js
@@ -1,6 +1,8 @@
 import 'dotenv/config';
 import express from "express";
 import cors from "cors";
+import http from "http";
+import { WebSocketServer } from "ws";
 import db from "./db.js";
 import { sendPortalLink, sendStatusUpdate } from "./email.js";
 import { registerOAuthRoutes } from "./oauth.js";
@@ -46,6 +48,36 @@ if (isProduction && (!JWT_SECRET || !SA_JWT_SECRET)) {
     process.exit(1);
 }
 
+// ── Real-time chat: WebSocket job-room registry ───────────────────────────
+// Maps jobId -> Set of live ws connections subscribed to that job's chat.
+// Populated in the wss "connection" handler set up near the bottom of this
+// file (after both http.createServer and WebSocketServer exist), but the
+// helpers live here so the REST message routes above can call them.
+const jobSubscribers = new Map();
+
+function subscribeToJob(jobId, ws) {
+    if (!jobSubscribers.has(jobId)) jobSubscribers.set(jobId, new Set());
+    jobSubscribers.get(jobId).add(ws);
+}
+
+function unsubscribeFromJob(jobId, ws) {
+    const set = jobSubscribers.get(jobId);
+    if (!set) return;
+    set.delete(ws);
+    if (set.size === 0) jobSubscribers.delete(jobId);
+}
+
+function broadcastToJob(jobId, payload) {
+    const set = jobSubscribers.get(jobId);
+    if (!set || set.size === 0) return;
+    const data = JSON.stringify(payload);
+    for (const client of set) {
+        if (client.readyState === client.OPEN) {
+            client.send(data);
+        }
+    }
+}
+
 // ── Security Middleware ───────────────────────────────────────────────────
 
 app.use(helmet({
@@ -909,7 +941,10 @@ app.post("/api/jobs/:id/messages", authenticateToken, (req, res) => {
             details: { content: (content || '').slice(0, 200) }
         });
 
-        res.status(201).json({ id, jobId, sender, content, timestamp });
+        const message = { id, jobId, sender, content, timestamp };
+        broadcastToJob(jobId, { type: "message", message });
+
+        res.status(201).json(message);
     } catch (error) {
         res.status(500).json({ error: isProduction ? "Internal Server Error" : error.message });
     }
@@ -1112,17 +1147,35 @@ app.post("/api/portal/:token/pay-final", (req, res) => {
 
 app.post("/api/portal/:token/messages", (req, res) => {
     const { token } = req.params;
-    const { sender, content } = req.body;
+    const { content } = req.body;
+    // SECURITY: this is a public, unauthenticated endpoint reachable by anyone with the
+    // portal link. Never trust a caller-supplied `sender` here — force it to "Client" so
+    // the portal cannot be used to spoof staff messages in the thread.
+    const sender = "Client";
     try {
-        const job = db.prepare("SELECT id, account_id FROM jobs WHERE secureToken = ?").get(token);
+        if (!content || typeof content !== "string" || !content.trim()) {
+            return res.status(400).json({ error: "Message content is required" });
+        }
+        const job = db.prepare("SELECT id, account_id, client FROM jobs WHERE secureToken = ?").get(token);
         if (!job) return res.status(404).json({ error: "Invalid link" });
 
         const id = uuidv4();
         const timestamp = new Date().toISOString();
+        const trimmedContent = content.trim().slice(0, 2000);
         db.prepare("INSERT INTO job_messages (id, job_id, sender, content, timestamp, account_id) VALUES (?, ?, ?, ?, ?, ?)")
-            .run(id, job.id, sender, content, timestamp, job.account_id);
-        
-        res.status(201).json({ id, jobId: job.id, sender, content, timestamp });
+            .run(id, job.id, sender, trimmedContent, timestamp, job.account_id);
+
+        appendProjectLog(job.account_id, job.client, job.id, {
+            type: 'message',
+            action: `Message sent by ${sender}`,
+            user: sender,
+            details: { content: trimmedContent.slice(0, 200) }
+        });
+
+        const message = { id, jobId: job.id, sender, content: trimmedContent, timestamp };
+        broadcastToJob(job.id, { type: "message", message });
+
+        res.status(201).json(message);
     } catch (error) {
         res.status(500).json({ error: isProduction ? "Internal Server Error" : error.message });
     }
@@ -1284,8 +1337,72 @@ if (isProduction) {
 // Purge logs older than 30 days on startup
 logger.rotateLogs(30);
 
-app.listen(PORT, '0.0.0.0', () => {
-    logger.info(`Backend server running on port ${PORT} in ${isProduction ? 'production' : 'development'} mode`);
+const server = http.createServer(app);
+
+// ── Real-time chat WebSocket endpoint ─────────────────────────────────────
+// Two ways to authenticate a connection, mirroring the REST auth model:
+//   - Client portal: ?token=<job.secureToken>            (no login — the link IS the credential)
+//   - Staff:          ?jobId=<id>&auth=<jwt>              (must belong to the job's account)
+const wss = new WebSocketServer({ server, path: "/ws" });
+
+wss.on("connection", (ws, req) => {
+    try {
+        const url = new URL(req.url, `http://${req.headers.host}`);
+        const portalToken = url.searchParams.get("token");
+        const jobIdParam = url.searchParams.get("jobId");
+        const authParam = url.searchParams.get("auth");
+
+        let jobId = null;
+
+        if (portalToken) {
+            const job = db.prepare("SELECT id FROM jobs WHERE secureToken = ?").get(portalToken);
+            if (!job) { ws.close(4004, "Invalid link"); return; }
+            jobId = job.id;
+            ws.role = "client";
+        } else if (jobIdParam && authParam) {
+            let decoded;
+            try {
+                decoded = jwt.verify(authParam, JWT_SECRET);
+            } catch (e) {
+                ws.close(4001, "Unauthorized");
+                return;
+            }
+            const job = db.prepare("SELECT id FROM jobs WHERE id = ? AND account_id = ?").get(jobIdParam, decoded.account_id);
+            if (!job) { ws.close(4003, "Forbidden"); return; }
+            jobId = job.id;
+            ws.role = "staff";
+        } else {
+            ws.close(4000, "Missing credentials");
+            return;
+        }
+
+        ws.jobId = jobId;
+        ws.isAlive = true;
+        subscribeToJob(jobId, ws);
+
+        ws.on("pong", () => { ws.isAlive = true; });
+        ws.on("close", () => unsubscribeFromJob(jobId, ws));
+        ws.on("error", () => unsubscribeFromJob(jobId, ws));
+    } catch (e) {
+        try { ws.close(1011, "Server error"); } catch (_) { /* noop */ }
+    }
+});
+
+// Drop dead connections (e.g. laptop sleep, dropped wifi) every 30s
+const wsHeartbeat = setInterval(() => {
+    wss.clients.forEach((ws) => {
+        if (ws.isAlive === false) {
+            unsubscribeFromJob(ws.jobId, ws);
+            return ws.terminate();
+        }
+        ws.isAlive = false;
+        ws.ping();
+    });
+}, 30000);
+wss.on("close", () => clearInterval(wsHeartbeat));
+
+server.listen(PORT, '0.0.0.0', () => {
+    logger.info(`Backend server running on port ${PORT} in ${isProduction ? 'production' : 'development'} mode (HTTP + WS)`);
 }).on('error', (err) => {
     if (err.code === 'EADDRINUSE') {
         logger.error(`Port ${PORT} is already in use.`);
diff --git a/src/components/ClientPortal.tsx b/src/components/ClientPortal.tsx
index 577137f6..05fb49b0 100644
--- a/src/components/ClientPortal.tsx
+++ b/src/components/ClientPortal.tsx
@@ -50,22 +50,49 @@ export function ClientPortal({
         };
 
         fetchData();
+    }, [token]);
 
-        // Polling for messages
-        const pollInterval = setInterval(async () => {
-            if (!token) return;
-            try {
-                const res = await fetch(`/api/portal/${token}`);
-                if (res.ok) {
-                    const data = await res.json();
-                    setMessages(data.messages || []);
+    // Real-time chat over WebSocket. The portal token itself is the credential —
+    // the server validates it against the job's secureToken before subscribing
+    // this connection to that job's message room.
+    React.useEffect(() => {
+        if (!token) return;
+        let ws: WebSocket | null = null;
+        let reconnectTimer: ReturnType<typeof setTimeout> | null = null;
+        let cancelled = false;
+
+        const connect = () => {
+            const protocol = window.location.protocol === "https:" ? "wss:" : "ws:";
+            ws = new WebSocket(`${protocol}//${window.location.host}/ws?token=${encodeURIComponent(token)}`);
+
+            ws.onmessage = (event) => {
+                try {
+                    const data = JSON.parse(event.data);
+                    if (data.type === "message" && data.message) {
+                        setMessages(prev => prev.some(m => m.id === data.message.id) ? prev : [...prev, data.message]);
+                    }
+                } catch (e) {
+                    console.error("WS message parse error:", e);
                 }
-            } catch (e) {
-                console.error("Polling error:", e);
-            }
-        }, 5000);
+            };
 
-        return () => clearInterval(pollInterval);
+            ws.onclose = () => {
+                if (cancelled) return;
+                reconnectTimer = setTimeout(connect, 3000);
+            };
+
+            ws.onerror = () => {
+                ws?.close();
+            };
+        };
+
+        connect();
+
+        return () => {
+            cancelled = true;
+            if (reconnectTimer) clearTimeout(reconnectTimer);
+            ws?.close();
+        };
     }, [token]);
 
     React.useEffect(() => {
@@ -80,7 +107,7 @@ export function ClientPortal({
 
         setIsSending(true);
         try {
-            const res = await fetch(`/api/jobs/${job.id}/messages`, {
+            const res = await fetch(`/api/portal/${token}/messages`, {
                 method: "POST",
                 headers: { "Content-Type": "application/json" },
                 body: JSON.stringify({
@@ -91,7 +118,7 @@ export function ClientPortal({
 
             if (res.ok) {
                 const sentMsg = await res.json();
-                setMessages(prev => [...prev, sentMsg]);
+                setMessages(prev => prev.some(m => m.id === sentMsg.id) ? prev : [...prev, sentMsg]);
                 setNewMessage("");
             }
         } catch (e) {
diff --git a/src/components/JobDetailView.tsx b/src/components/JobDetailView.tsx
index 7176f1d2..4332366a 100644
--- a/src/components/JobDetailView.tsx
+++ b/src/components/JobDetailView.tsx
@@ -59,8 +59,52 @@ export function JobDetailView({
     };
 
     fetchMessages();
-    const poll = setInterval(fetchMessages, 5000);
-    return () => clearInterval(poll);
+  }, [job.id]);
+
+  // Real-time chat over WebSocket, authenticated with the staff JWT and scoped
+  // server-side to jobs belonging to that account (mirrors the REST auth check).
+  React.useEffect(() => {
+    const authToken = localStorage.getItem('token');
+    if (!authToken) return;
+
+    let ws: WebSocket | null = null;
+    let reconnectTimer: ReturnType<typeof setTimeout> | null = null;
+    let cancelled = false;
+
+    const connect = () => {
+      const protocol = window.location.protocol === "https:" ? "wss:" : "ws:";
+      ws = new WebSocket(
+        `${protocol}//${window.location.host}/ws?jobId=${encodeURIComponent(job.id)}&auth=${encodeURIComponent(authToken)}`
+      );
+
+      ws.onmessage = (event) => {
+        try {
+          const data = JSON.parse(event.data);
+          if (data.type === "message" && data.message) {
+            setMessages(prev => prev.some(m => m.id === data.message.id) ? prev : [...prev, data.message]);
+          }
+        } catch (e) {
+          console.error("WS message parse error:", e);
+        }
+      };
+
+      ws.onclose = () => {
+        if (cancelled) return;
+        reconnectTimer = setTimeout(connect, 3000);
+      };
+
+      ws.onerror = () => {
+        ws?.close();
+      };
+    };
+
+    connect();
+
+    return () => {
+      cancelled = true;
+      if (reconnectTimer) clearTimeout(reconnectTimer);
+      ws?.close();
+    };
   }, [job.id]);
 
   React.useEffect(() => {
@@ -150,7 +194,7 @@ export function JobDetailView({
       });
       if (res.ok) {
         const sent = await res.json();
-        setMessages(prev => [...prev, sent]);
+        setMessages(prev => prev.some(m => m.id === sent.id) ? prev : [...prev, sent]);
         setNewChatMessage("");
       }
     } catch (e) {
diff --git a/vite.config.ts b/vite.config.ts
index f3804d23..87ac8a42 100644
--- a/vite.config.ts
+++ b/vite.config.ts
@@ -23,7 +23,8 @@ export default defineConfig(({ mode }) => {
         ignored: ['**/data.db', '**/data.db-journal', '**/data.db-shm', '**/data.db-wal', '**/.env'],
       },
       proxy: {
-        '/api': 'http://127.0.0.1:3001',
+        '/api': { target: 'http://127.0.0.1:3001', ws: true },
+        '/ws': { target: 'http://127.0.0.1:3001', ws: true },
       },
     },
     build: {
